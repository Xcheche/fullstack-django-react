"""Tests for the `user` app.

This module is intentionally minimal. Add unit and integration tests
for serializers, viewsets and manager helpers to validate behaviour
when making changes. Example tests might cover registration, login
flows and `get_object_by_public_id` error cases.
"""

from django.test import TestCase


class PlaceholderTest(TestCase):
	"""A placeholder test to remind contributors to add tests."""
	def test_placeholder(self):
		self.assertTrue(True)
